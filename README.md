# react-boilerplate

Personal React boilerplate.

  - react-router
  - sass or styled-components (`/styled-components` banch)
  - hot-module-replace

### Installation

```sh
$ npm install
$ npm run start
```

### Build

```sh
$ npm install
$ npm run build
```
